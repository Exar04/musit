syntax = "proto3";

message Person {
  string username = 1;
  string email = 2;
  string userid = 3;
}

message RegisterResponse {
  string message = 1;
}

service UserService {
  rpc RegisterUser(Person) returns (RegisterResponse);
}